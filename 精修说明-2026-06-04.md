# 精修说明 - 2026-06-04

> **本次修复前提**：已对照 Claude Code 官方环境变量文档（code.claude.com/docs/en/env-vars）逐项核实。
>
> **重要更正**：上一版"分析"是在未真读取文件的情况下基于常识推断给出的，**有 1 个错误**：`DISABLE_INSTALLATION_CHECKS` 和 `DISABLE_GROWTHBOOK` 是仓库作者**自创的**（不在官方列表里），不是我之前说的"应该是官方少列了"。这两个变量名 Claude Code 不认，**等于气隙没关严**。本次已删除。

---

## 修复的硬伤（按优先级）

### P0-1 鉴权头错（401 必现）

| 位置 | 改动 |
|------|------|
| `.env.template` 行 15 | `ANTHROPIC_AUTH_TOKEN` → `ANTHROPIC_API_KEY` |
| `run.bat` 行 29-32 | 检查名同步改 |
| `setup.bat` 行 48-51 | 检查名同步改 |
| `verify_airgap.bat` 行 71、74 | 检查名同步改 |
| `README.md` 行 121、201、217 | 文档同步改，并加行 96 解释为什么 |

**原理**：
- `ANTHROPIC_API_KEY` → 发 `x-api-key: xxx` 头
- `ANTHROPIC_AUTH_TOKEN` → 发 `Authorization: Bearer xxx` 头
- New API 的 `/v1/messages` 端点走 Anthropic 协议，**期望 x-api-key**

### P0-2 三个"假"环境变量

| 位置 | 旧值 | 新值 |
|------|------|------|
| `.env.template` 行 28 | `DISABLE_NONESSENTIAL_TRAFFIC=1` | `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1`（官方合集开关） |
| `.env.template` 行 37 | `DISABLE_INSTALLATION_CHECKS=1` | **删除**（官方不存在） |
| `.env.template` 行 40 | `DISABLE_GROWTHBOOK=1` | **删除**（官方不存在） |
| `run.bat` 行 41、44、45 | 同步修改 | |

### P0-3 漏设的官方变量

| 位置 | 新增 |
|------|------|
| `.env.template`、`run.bat` | 补 `DISABLE_FEEDBACK_COMMAND=1`（避免 /feedback 命令回传） |
| `.env.template`、`run.bat` | 补 `DO_NOT_TRACK=1`（通用 DNT 信号） |

---

## 精简项

### claude_settings.json

```diff
- {
-   "env": { "ANTHROPIC_MODEL": "GLM-5.1-AWQ-4bit" },
-   "permissions": {
-     "defaultMode": "acceptEdits",
-     "allow": ["Bash(*)", "Read(*)", "Write(*)", "Edit(*)", "Glob(*)", "Grep(*)"]
-   },
-   "model": "GLM-5.1-AWQ-4bit"
- }
+ {
+   "permissions": {
+     "defaultMode": "default",
+     "allow": ["Read(*)", "Glob(*)", "Grep(*)"]
+   },
+   "model": "GLM-5.1-AWQ-4bit"
+ }
```

**为什么改**：
1. 删 `env.ANTHROPIC_MODEL`：settings.json 的 `env` 段不生效（Claude Code 不认这个字段），与 .env 重复。
2. 删 `Bash(*)/Write(*)/Edit(*)`：内网环境用 `acceptEdits` 配合全开放 = 失去审计保护。改为 `default` 模式，只允许只读工具，编辑/写文件/执行命令需用户手动确认。
3. 保留 `model` 字段：这是 settings.json 真正生效的字段（与 `claude --model` 等价）。

### dependencies/SHA256SUMS.txt

```diff
  ebd318e1d3ee0cc1ac8ead026f1edf8678dcb42c7d74d757b8e2fa8a1be0b25f  Git-2.51.2-64-bit.exe
  cc0ff0eb1dc3f5188ae6300faef32bf5beeba4bdd6e8e445a9184072096b713b  vc_redist.x64.exe
+ ac4e1319f6ac0c9e04336d0ae5845acc5dd317bf9e6f26ca47e63df06d91f8bc  claude.exe
```

`校验.bat` 和 `verify_airgap.bat` 里 SHA256 还是硬编码的，本次未改——下一版可以考虑从 SHA256SUMS.txt 读取。

---

## 离线部署 SOP（3 步走）

> 适用：完全气隙内网 Windows x64 机器

### Step 1: 准备 U 盘（联网机器做一次）

```
D:\claude-airgap-usb\
├── claude.exe                              ← 239MB，根目录
├── dependencies\
│   ├── Git-2.51.2-64-bit.exe               ← 63MB
│   └── vc_redist.x64.exe                   ← 24MB
├── .env                                    ← 填好的（见 Step 2）
├── .env.template                           ← 模板
├── claude_settings.json
├── manifest.json
├── setup.bat
├── run.bat
├── install_settings.bat
├── install_deps.bat
├── verify_airgap.bat
├── 校验.bat
├── uninstall.bat
├── LICENSE
├── README.md
├── 精修说明-2026-06-04.md                  ← 本文件
└── .gitignore
```

**下载 3 个 .exe**：
| 文件 | 来源 | SHA256 |
|------|------|--------|
| `claude.exe` | https://downloads.claude.ai/claude-code-releases/2.1.162/win32-x64/claude.exe | ac4e1319f6ac0c9e04336d0ae5845acc5dd317bf9e6f26ca47e63df06d91f8bc |
| `Git-2.51.2-64-bit.exe` | https://registry.npmmirror.com/-/binary/git-for-windows/v2.51.2.windows.1/Git-2.51.2-64-bit.exe | ebd318e1d3ee0cc1ac8ead026f1edf8678dcb42c7d74d757b8e2fa8a1be0b25f |
| `vc_redist.x64.exe` | https://aka.ms/vs/17/release/vc_redist.x64.exe | cc0ff0eb1dc3f5188ae6300faef32bf5beeba4bdd6e8e445a9184072096b713b |

### Step 2: 准备 .env（U 盘里改）

复制 `.env.template` → `.env`，编辑填 3 个值：

```ini
ANTHROPIC_BASE_URL=http://10.x.x.x:3000/v1   ← New API 实际地址
ANTHROPIC_API_KEY=sk-xxxxxxxxxxxxxx         ← New API 控制台生成的 Key
ANTHROPIC_MODEL=GLM-5.1-AWQ-4bit            ← 已经在模板里，不用改
```

### Step 3: 在内网 Windows 上执行（3 条命令）

```cmd
X:                            REM 进 U 盘
setup.bat                     REM 一键装依赖+校验+探测
run.bat                       REM 启动 Claude Code
```

`setup.bat` 会自动：
1. 检测/安装 Git + VC++
2. 校验 claude.exe SHA256
3. 检查 .env 必填项
4. 探测 New API 可达性
5. 验证外网已隔离（HEAD downloads.claude.ai 应失败）

`run.bat` 会自动：
1. 加载 .env
2. **强制注入 6 个 DISABLE_* + CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC + DO_NOT_TRACK**（即使 .env 漏配也不触网）
3. 启动 claude.exe

---

## 文件清单

| 文件 | 作用 | 大小（行） |
|------|------|-----------|
| README.md | 完整文档 | 247 |
| 精修说明-2026-06-04.md | 本次修复说明 + 部署 SOP | - |
| .env.template | 环境变量模板 | 48 |
| .gitignore | 排除 .env 和 *.exe | - |
| claude_settings.json | 用户级 settings（只读权限） | 11 |
| manifest.json | 官方版本清单 | 47 |
| setup.bat | 一键安装入口 | 104 |
| run.bat | 启动器（含气隙兜底） | 48 |
| install_settings.bat | 推送 settings.json 到 %USERPROFILE%\.claude\ | 22 |
| install_deps.bat | 单独装 Git + VC++ | 68 |
| verify_airgap.bat | 5 步气隙验证 | 107 |
| 校验.bat | 轻量 SHA256 校验 | 31 |
| uninstall.bat | 卸载 | 25 |
| dependencies/SHA256SUMS.txt | 3 个 .exe 的 SHA256 | 3 |
| **合计** | **12 个脚本/配置 + 2 个文档** | **~700 行** |

> 暂未精简：每个 .bat 职责单一，合并反而让单文件变臃肿。`校验.bat` 和 `verify_airgap.bat` 有功能重叠但速度/详细度不同（前者 1 步、后者 5 步），保留两个让用户按需选。

---

## 验证清单（部署完跑一遍）

```cmd
verify_airgap.bat
```

应该看到 5/5 PASS：
- [OK] Git 已装
- [OK] VC++ 已装
- [OK] claude.exe SHA256 校验通过
- [OK] ANTHROPIC_API_KEY 已配置（非占位符）
- [OK] 外网已隔离（HEAD downloads.claude.ai 失败）

再跑一遍：
```cmd
校验.bat
```

应该看到 `[OK] 校验通过`。
